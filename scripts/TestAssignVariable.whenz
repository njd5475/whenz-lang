#!/usr/bin/env whenz

when event app_starts do
	print Starting Test: Assign Variable
	@setMe = 10
	@setOther = @setMe
	trigger event done
	
when @setOther == 10 do
	print setOther has been set to 10
	print Test Passed.
	
when event done do
	exit