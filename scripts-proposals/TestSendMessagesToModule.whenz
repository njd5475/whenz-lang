
when event app_starts do
    print Starting 'Send Messages To Module' Test
    @receiver = launch TestSendMessageModule with
        @messagesExpected = 100
    end

    @messageCounter = 0

when @messageCounter <= 100 do
    increment @messageCounter
    trigger messageFromMainModule on @receiver with
        @messageFromMainModule = Message number @messageCounter
    end
